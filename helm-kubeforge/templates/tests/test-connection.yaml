apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-kubeforge.fullname" . }}-test-connection"
  labels:
    {{- include "helm-kubeforge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-kubeforge.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
